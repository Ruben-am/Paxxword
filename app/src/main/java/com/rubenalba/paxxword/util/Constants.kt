package com.rubenalba.paxxword.util

object Constants {
    // Token de validación por defecto para la migración SQL
    const val VERIFICATION_TOKEN_DEFAULT = "PAXXWORD_VERIFIED_USER"

    // Claves de DataStore
    const val PREF_THEME_KEY = "app_theme"
    const val PREF_LANGUAGE_KEY = "app_language"

    // Tipos de archivo MIME
    const val MIME_TYPE_JSON = "application/json"
}